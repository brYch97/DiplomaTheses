const customConfig = {
    // watch: true, // uncomment this line to enable webpack watch mode
    mode: 'development',
    devtool: "inline-source-map"
}
// module.exports = customConfig; // uncoment this line to enable local debugging